# Claude Code Entry

Shared project rules and the current evidence route live in `AGENTS.md`.

@import AGENTS.md

If Claude Code leaves `@import` as literal text, read `AGENTS.md` and the latest
work log it names before editing.
